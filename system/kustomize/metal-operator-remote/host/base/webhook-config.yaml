apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-config
data:
  webhooks.yaml: |
    # Content populated from remote/upstream/webhooks/manifests-url-based.yaml
    # This ConfigMap is consumed by webhook-injector to configure admission webhooks
    # in the remote virtual cluster
    PLACEHOLDER
