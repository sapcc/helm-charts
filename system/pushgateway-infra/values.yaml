global:
  domain: cloud.sap
  region: DEFINED-IN-REGION
fullnameOverride: "pushgateway-infra"
prometheus-pushgateway:
  enabled: false
  ingress:
    enabled: true
    hosts:
      - pushgateway-infra.scaleout
    vicePresident: true
    tls:
      enabled: true
