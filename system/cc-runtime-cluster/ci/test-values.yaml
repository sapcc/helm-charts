global:
  region: test-1

machineTemplate:
  metadata:
    node-role.kubernetes.io/cp: ""

controlplane:
  machineTemplate:
    matchLabels:
      node-role.kubernetes.io/cp: ""

worker:
  machineTemplate:
    matchLabels:
      node-role.kubernetes.io/worker: ""

additionalConfig:
  secrets: |
    - path: /etc/kubernetes/enc/enc.yaml
      filesystem: root
      mode: 0644
      overwrite: true
      contents:
        inline: |
          Foo Bar
