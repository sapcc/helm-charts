{{- if .Values.thanos.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: thanos-query
  name: thanos-query
spec:
  ports:
  - name: http-query
    port: 9090
    targetPort: http
  selector:
    app: thanos-query
{{ end }}
