{{- if .Values.thanos.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: thanos-compactor
  labels:
    app: thanos-compactor
spec:
  ports:
  - name: http
    port: 9090
    targetPort: http
  selector:
    app: thanos-compactor
{{- end }}
