{{- if logstash.tls.enabled }}
apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: tls-logstash

data:
  tls.crt: {{ logstash.tls.crt | b64enc }}
  tls.key: {{ logstash.tls.key | b64enc }}
---
{{- end}}
