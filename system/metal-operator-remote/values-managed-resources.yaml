# Applied against metal-operator-core's own upstream values.yaml schema (see build-metal-operator-remote in system/Makefile), not this chart's values.yaml.
manager:
  enabled: false
rbac:
  namespaced: false
serviceAccount:
  enable: true
crd:
  enable: true
  keep: false
metrics:
  enable: true
webhook:
  enable: true
prometheus:
  enable: false
certManager:
  enable: false
