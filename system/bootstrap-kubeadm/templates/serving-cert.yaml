apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "bootstrap-kubeadm.fullname" . }}-serving-cert
  labels:
  {{- include "bootstrap-kubeadm.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "bootstrap-kubeadm.fullname" . }}-{{ .Release.Namespace }}-kubeadm-bootstrap-webhook-service.{{
    .Release.Namespace }}-kubeadm-bootstrap-system.svc'
  - '{{ include "bootstrap-kubeadm.fullname" . }}-{{ .Release.Namespace }}-kubeadm-bootstrap-webhook-service.{{
    .Release.Namespace }}-kubeadm-bootstrap-system.svc.{{ .Values.kubernetesClusterDomain
    }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "bootstrap-kubeadm.fullname" . }}-selfsigned-issuer'
  secretName: capi-kubeadm-bootstrap-webhook-service-cert
  subject:
    organizations:
    - k8s-sig-cluster-lifecycle
