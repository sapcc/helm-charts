image:
  repository: keppel.eu-de-1.cloud.sap/ccloud/vpa_butler

# The update mode for vertical pod autoscaler resources.
# Allowed values:
# 1) Off (default; recommendations only)
# 2) Initial (requires admission webhook)
# 3) Recreate (requires updater)
updateMode: "Off"
