apiVersion: v1
kind: Secret
metadata:
  name: {{ required ".Values.kubernikus.backup.secretName" .Values.kubernikus.backup.secretName }}
type: Opaque
data:
  bucket-name: {{ required ".Values.kubernikus.backup.bucketName" .Values.kubernikus.backup.bucketName }}
  region: {{ required ".Values.kubernikus.backup.region" .Values.kubernikus.backup.region }}
  access-key-id: {{ required ".Values.kubernikus.backup.accessKeyId" .Values.kubernikus.backup.accessKeyId }}
  secret-access-key: {{ required ".Values.kubernikus.backup.secretAccessKey" .Values.kubernikus.backup.secretAccessKey }}
