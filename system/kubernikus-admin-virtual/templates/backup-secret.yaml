apiVersion: v1
kind: Secret
metadata:
  name: {{ required ".Values.backup.secretName" .Values.backup.secretName }}
type: Opaque
data:
  bucket-name: {{ required ".Values.backup.bucketName" .Values.backup.bucketName }}
  region: {{ required ".Values.backup.region" .Values.backup.region }}
  access-key-id: {{ required ".Values.backup.accessKeyId" .Values.backup.accessKeyId }}
  secret-access-key: {{ required ".Values.backup.secretAccessKey" .Values.backup.secretAccessKey }}
