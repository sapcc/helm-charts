apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: rule-missing-severity
  namespace: default
spec:
  groups:
    - name: example.alerts
      rules:
        - alert: SomethingWrong
          expr: up == 0
          labels: {}
          annotations: {}
