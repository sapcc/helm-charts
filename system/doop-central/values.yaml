image_tag: cef42a6579 # Git commit for <https://github.com/sapcc/gatekeeper-addons>

# NOTE: This section is shared with the `system/gatekeeper` chart.
doop:
  # values with default value null must be provided by values.yaml
  swift_password: null
