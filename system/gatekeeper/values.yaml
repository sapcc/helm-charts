# documentation for this section: <https://github.com/open-policy-agent/gatekeeper/tree/master/charts/gatekeeper>
gatekeeper-upstream:
  createNamespace: false
  constraintViolationsLimit: 100
  disableValidatingWebhook: true # TODO enable
  logDenies: true

  audit:
    resources:
      limits:
        cpu: 2
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 256Mi

# Must be provided via --set-string; e.g. "qa-de-1" for QA controlplane or "s-qa-de-1" for QA scaleout.
cluster_name: null

helm_manifest_parser:
  image_tag: 036432907d # Git commit for <https://github.com/sapcc/gatekeeper-addons>

doop_agent:
  enabled: true
  image_tag: 626eb533c2 # Git commit for <https://github.com/sapcc/gatekeeper-addons>

# NOTE: This section is shared with the `system/doop-central` chart.
doop:
  # values with default value null must be provided by values.yaml
  swift_region: null
  swift_password: null
