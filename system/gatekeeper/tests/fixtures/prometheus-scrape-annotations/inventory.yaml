apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: openstack
  namespace: dummy
  labels:
    support-group: foo-group
    service: dummy
spec:
  podMonitorSelector:
    matchLabels:
      prometheus: dummy
