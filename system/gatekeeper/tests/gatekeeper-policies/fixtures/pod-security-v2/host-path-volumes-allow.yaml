apiVersion: v1
kind: Pod
metadata:
  name: host-path-volumes-allow
  namespace: test-allow-all
spec:
  containers:
  - name: foo-container
    image: foo.image-registry.com/test-repo/dummy:20250519150208
    volumeMounts:
    - mountPath: /hostfs
      name: hostfs
  volumes:
  - name: hostfs
    hostPath:
      path: /
