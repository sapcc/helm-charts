apiVersion: v2
name: sci-agent
description: Service account and RBAC for SCI Agent (read-only K8s access)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: owner-info
    version: ">=1.0.0"
    repository: oci://keppel.eu-de-1.cloud.sap/ccloud-helm
