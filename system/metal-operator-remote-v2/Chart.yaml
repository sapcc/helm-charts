apiVersion: v2
name: metal-operator-remote-v2
description: |
  Operator-native Helm chart for metal-operator remote deployment.
  Rendered twice by the dual-deployment-operator (mode=seed, mode=shoot).
  The chart MUST NOT set .Values.mode - the operator injects it.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: metal-operator
    repository: oci://ghcr.io/ironcore-dev/charts
    version: 0.6.2-crds
    alias: metal-operator-core
