apiVersion: v1
kind: Service
metadata:
  labels:
    app: node-problem-detector
  name: node-problem-detector
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: metrics
    port: 9102
    protocol: TCP
  selector:
    app: node-problem-detector
