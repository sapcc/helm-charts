apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: {{ template "name" . }}

data:
  vice.cert: {{ default "" .Values.vice.cert | quote }}
  vice.key: {{ default "" .Values.vice.key | quote }}
