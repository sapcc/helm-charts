ingress:
  external_service_ip: DEFINED-AS-VALUE

cert-manager:
  # TODO: remove once testing is done
  replicaCount: 0

  image:
    repository: sapcc/cert-manager-controller
    # TODO: fix tag, pullPolicy after testing
    tag: build
    pullPolicy: Always

  ingressShim:
    extraArgs:
      - --default-issuer-name=letsencrypt-prod
      - --default-issuer-kind=ClusterIssuer


cert_issuer:
  enabled: false
# define at least one issuer
#  acme:
#    server:
#    email:
#
#  designate:
#    authURL:
#    regionName:
#    userName:
#    userDomainName:
#    password:
#    projectName:
#    projectDomainName:
