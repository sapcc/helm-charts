{{- if .Values.prometheus_server.enabled }}
apiVersion: v1
kind: Secret

metadata:
  name: {{ .Values.prometheus_server.name }}-additional-scrape-config
  labels:
    prometheus: {{ .Values.prometheus_server.name }}

data:
    scrape-config.yaml: |-
#      {{ .Files.Get "charts/snmp-exporter/_snmp-exporter-arista.yaml" }}
      {{ include (print $.Template.BasePath "/_snmp-exporter-arista.yaml") . | b64enc }}
# {{ .Files.Glob "_snmp-exporter-*.yaml" }}
{{- end }}
