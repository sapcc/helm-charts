apiVersion: v1
data:
  username: {{ .Values.http_sd_configs.basicAuth.jumpserver.otel.username | b64enc }}
  password: {{ .Values.http_sd_configs.basicAuth.jumpserver.otel.password | b64enc }}
kind: Secret
metadata:
  annotations:
  name: jumpserver-otel-exporter-auth
  namespace: infra-monitoring
type: Opaque
