apiVersion: v1
kind: Service
metadata:
  name: cablecheck-exporter
  labels:
    app: {{ include "cablecheck-exporter.name" . }}
    chart: {{ include "cablecheck-exporter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    system: infra_monitoring
spec:
  selector:
    app: {{ include "cablecheck-exporter.name" . }}
  ports:
    - port: 9100
