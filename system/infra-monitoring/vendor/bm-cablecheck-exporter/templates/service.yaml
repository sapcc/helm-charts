apiVersion: v1
kind: Service
metadata:
  name: bm-cablecheck-exporter
  labels:
    app: {{ include "bm-cablecheck-exporter.name" . }}
    chart: {{ include "bm-cablecheck-exporter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    system: infra_monitoring
spec:
  selector:
    app: {{ include "bm-cablecheck-exporter.name" . }}
  ports:
    - port: 9100
