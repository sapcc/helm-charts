apiVersion: v1
kind: Service
metadata:
  name: bm_cablecheck_exporter
  labels:
    app: {{ include "bm_cablecheck_exporter.name" . }}
    chart: {{ include "bm_cablecheck_exporter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    system: infra_monitoring
spec:
  selector:
    app: {{ include "bm_cablecheck_exporter.name" . }}
  ports:
    - port: 9100
