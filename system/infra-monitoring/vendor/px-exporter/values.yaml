alerts:
  enabled: SET-IN-REGION
  # Keys = directory names in alerts/ and aggregations/
  prometheus:
    - name: infra-collector
      type: prometheus
    - name: kubernetes
      type: prometheus
