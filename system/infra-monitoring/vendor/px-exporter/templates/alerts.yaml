{{ template "prometheus.alerts" . }}

{{- $values := .Values -}}
{{- if $values.px_exporter.px.alerts.enabled }}
{{- range $path, $bytes := .Files.Glob "px.alerts/*.alerts" }}
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule

metadata:
  name: {{ printf "px-exporter-%s" $path | replace "/" "-" }}
  labels:
    app: snmp-exporter
    prometheus: {{ required ".Values.px_exporter.alerts.prometheus missing" $values.px_exporter.alerts.prometheus }}

spec:
{{ printf "%s" $bytes | indent 2 }}

{{- end }}
{{- end }}
