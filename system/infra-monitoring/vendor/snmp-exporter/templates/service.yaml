kind: Service
apiVersion: v1
metadata:
  name: snmp-exporter
  labels:
    system: openstack
    service: metrics
    component: snmp-exporter

spec:
  selector:
    component: snmp-exporter
  ports:
    - name: metrics
      port: {{.Values.listen_port}}
