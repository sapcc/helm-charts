apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-usage-exporter.fullname" . }}
  labels:
    {{- include "image-usage-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-usage-exporter.selectorLabels" . | nindent 4 }}
