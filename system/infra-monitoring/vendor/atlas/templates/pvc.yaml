apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atlas-targets
  namespace: infra-monitoring
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
