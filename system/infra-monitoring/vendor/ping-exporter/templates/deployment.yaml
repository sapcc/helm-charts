{{- if .Values.enabled }}

kind: Deployment
apiVersion: extensions/v1beta1

metadata:
  name: ping-exporter

spec:
  replicas: 1
  templates:
    spec:
      containers:
        - name: ping-exporter
          image: czerwonk/ping_exporter:latest
          imagePullPolicy: Always
{{ end -}}