{{- if .Values.enabled }}
{{- range $app := .Values.apps }}

kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ required "Value apps[].name" $app.name }}
data:
  netapp-filers.yaml: "TO BE POPULATED BY SIDECAR"

{{- end }}
{{- end }}
