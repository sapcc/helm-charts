# Default values for netapp-cap-exporter.

apps:
  - name: "netapp-exporter-manila"
    netappsd:
      query: md
  - name: "netapp-exporter-cinder"
    netappsd:
      query: bb
  - name: "netapp-exporter-bare-metal"
    netappsd:
      query: bm
  - name: "netapp-exporter-control-plane"
    netappsd:
      query: cp

listenPort: 9108

exporter:
  image:
    repository: hub.global.cloud.sap/monsoon/netapp-api-exporter
    tag: c7581011
    pullPolicy: IfNotPresent
  resources:
    requests:
      memory: 200Mi
      cpu: 100m

netappsd:
  image: 
    repository: hub.global.cloud.sap/monsoon/netappsd
    tag: master-0ae024a1
    pullPolicy: IfNotPresent
  resources:
    requests:
      memory: 50Mi
      cpu: 50m
  query: bb

service:
  type: ClusterIP

metrics:
  scrape: true
  targets: infra-collector
