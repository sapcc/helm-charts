windows_exporter:
  enabled: true
  basic_auth:
    enabled: false
    username: # Defined via secrets.
    password: # Defined via secrets.
  scrapeInterval: 4m
  scrapeTimeout: 120s
  listen_port: 9200
  scheme: HTTP
