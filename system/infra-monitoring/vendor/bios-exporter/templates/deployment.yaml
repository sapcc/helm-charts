apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: bios-exporter
  namespace: infra-monitoring
  #labels:
    #system: openstack
    #service: metrics

spec:
  template:
    metadata:
      labels:
        component: bios-exporter
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.bios_exporter.listen_port | quote }} 
    spec:
      containers:
         - name: bios-exporter
           image: {{ required "global.imageRegistry variable missing" .Values.global.imageRegistry }}/{{ .Values.bios_exporter.image.namespace }}/{{ .Values.bios_exporter.image.name }}:{{ .Values.bios_exporter.image.tag }}
           ports:
             - name: metrics
               containerPort: {{ .Values.bios_exporter.listen_port }}
           command:
             - /bios_exporter/export.py
           args:
             - -o {{ .Values.bios_exporter.listen_port | quote }}
           env:
             - name: REMOTEUSER
               value: {{ .Values.bios_exporter.user }}
             - name: REMOTEPW
               value: {{ .Values.bios_exporter.userpw }}

