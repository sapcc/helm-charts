global:
  region: bogus
prometheus-server:
  name: ci-test-name

http_sd_configs:
  tls:
    jump01:
      crt: foo
      key: foo2
      username: some_user
      password: nothing
    jump02:
      crt: foo3
      key: foo4
      username: some_user1
      password: nothing2
