global:
  region: bogus
prometheus-server:
  name: ci-test-name

http_sd_configs:
  basicAuth:
    jumpserver:
      username: some_user
      password: nothing
      otel:
        username: some_user
        password: nothing
  tls:
    jump01:
      crt: foo
      key: foo2
    jump02:
      crt: foo3
      key: foo4

windows_exporter:
  basic_auth:
    enabled: true
    username: prom-user
    password: prom-password
