global:
  region: bogus
  tld: test.local
prometheus_infra_collector:
  thanosSeeds:
    swiftStorageConfig:
      userDomainName: test-domain
      domainName: test-domain
      password: test-password
      userName: test-user
      projectName: test-project
      authURL: https://test.example.com

prometheus-server:
  name: ci-test-name

http_sd_configs:
  basicAuth:
    jumpserver:
      username: some_user
      password: nothing
      otel:
        username: some_user
        password: nothing
  tls:
    jump01:
      crt: foo
      key: foo2
    jump02:
      crt: foo3
      key: foo4

windows_exporter:
  enabled: true
  basic_auth:
    enabled: true
    username: prom-user
    password: prom-password
  alerts:
    enabled: true
