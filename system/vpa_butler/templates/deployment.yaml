apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: vpa_butler
  name: vpa_butler
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vpa_butler
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vpa_butler
    spec:
      containers:
      - image: {{ include "image" . }}
        imagePullPolicy: IfNotPresent
        name: vpa_butler
        resources:
          limits:
            cpu: 100m
            memory: 50Mi
          requests:
            cpu: 50m
            memory: 10Mi
        serviceAccountName: vpa_butler
