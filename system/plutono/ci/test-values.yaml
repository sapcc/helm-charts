grafana:
  gitsync:
    repository: 'just_for_linting'

postgresql:
  nameOverride: '' # nameOverride is only allowed in upstream chart for release names starting with "plutono", but `helm lint` autogenerates a different release name

global:
  registry: 'just_for_linting'
  dockerHubMirror: 'just_for_linting'
