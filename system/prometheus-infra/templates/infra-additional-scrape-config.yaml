apiVersion: v1
kind: Secret

metadata:
  name: infra-additional-scrape-config
  labels:
    prometheus: infra

data:
  scrape-config.yaml: {{ include (print $.Template.BasePath  "/_infra-prometheus.yaml.tpl") . | b64enc }}
