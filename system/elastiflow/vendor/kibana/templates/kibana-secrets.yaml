apiVersion: v1
kind: Secret
metadata:
  name: kibana-secrets
  namespace: elastiflow
  labels:
    app: elastiflow
    release: elastiflow
data:
  elastic_password: {{ .Values.global.elastiflow_admin_password | b64enc | quote }}
