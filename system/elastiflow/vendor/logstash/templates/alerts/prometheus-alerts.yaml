apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule

metadata:
  name: {{ include "logstash.fullName" . }}-prometheus.alerts
  labels:
    prometheus: {{ required ".Values.prometheus missing"  .Values.alerts.prometheus }}

spec:
{{ include (print .Template.BasePath "/alerts/_prometheus.alerts.tpl") . | indent 2 }}
