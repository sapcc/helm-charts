{{- if .Values.filebeat.enabled}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "elastiflow-filebeat"
  labels:
    app: "elastiflow-filebeat"
    chart: "{{ .Chart.Name }}"
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
spec:
  selector:
    matchLabels:
      app: elastiflow-filebeat
  replicas: 1
  template:
    metadata:
      labels:
        app: elastiflow-filebeat
        chart: "{{ .Chart.Name }}"
        heritage: {{ .Release.Service | quote }}
        release: {{ .Release.Name | quote }}
    spec:
      containers:
      - name: filebeat
        image: keppel.eu-de-1.cloud.sap/ccloud/elastiflow-filebeat:202103081700
        ports:
          - containerPort: 2055
            protocol: UDP
        env:
          - name: LOGSTASH_HOSTS
            value: {{ .Values.filebeat.hosts | quote }}
          - name: NUM_WORKERS
            value: {{ .Values.filebeat.workers | quote}}
          - name: LOGSTASH_LB
            value: {{ .Values.filebeat.loadbalance }}
          - name: LOGSTASH_TTL
            value: {{ .Values.filebeat.ttl }}
          - name: LOGSTASH_PIPELINING
            value: {{ .Values.filebeat.pipelining }}
{{- end }}