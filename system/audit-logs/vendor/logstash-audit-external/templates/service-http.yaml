apiVersion: v1
kind: Service
metadata:
  name: logstash-audit-external-http
  labels:
    component: logstash-audit-external
spec:
  ports:
  - name: https
    protocol: TCP
    port: {{.Values.input_http_port}}
  selector:
    component: logstash-audit-external
