{{- if and .Values.rbac.create (not .Values.remote.enabled) }}
{{ include "disco.roleBinding.leaderElection" (dict "namespace" .Release.Namespace "serviceAccountName" .Values.serviceAccount.name) }}
{{- end }}
