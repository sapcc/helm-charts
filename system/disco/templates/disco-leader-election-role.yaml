{{- if and .Values.rbac.create (not .Values.remote.enabled) }}
{{ include "disco.role.leaderElection" (dict "namespace" .Release.Namespace "serviceAccountName" .Values.serviceAccount.name) }}
{{- end }}
