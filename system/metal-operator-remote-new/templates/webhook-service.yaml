{{- if .Values.webhook.enable }}
apiVersion: v1
kind: Service
metadata:
  name: metal-operator-webhook-service
  labels:
    {{- include "metal-operator-remote-new.labels" . | nindent 4 }}
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-manager
{{- end }}
