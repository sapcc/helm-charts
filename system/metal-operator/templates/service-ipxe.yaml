apiVersion: v1
kind: Service
metadata:
  name: ipxe-http
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: ipxe
    port: 5000
    protocol: TCP
    targetPort: 5000
    nodePort: {{ .Values.ipxe.port }}
  selector:
    app.kubernetes.io/name: ipxe-operator
  type: NodePort
