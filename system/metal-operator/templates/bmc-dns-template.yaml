apiVersion: v1
kind: ConfigMap
metadata:
  name: dns-template
  namespace: { { .Release.Namespace } }
data:
  bmc_dns.yaml:
    { { include (print .Template.BasePath "/etc/_dns-record-template.yaml") } }
