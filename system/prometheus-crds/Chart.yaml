apiVersion: v2
name: prometheus-crds
description: A Helm chart containing Prometheus CRDs.
type: application
version: 3.0.2
# Before bumping the chart version above ensure all CRDs are on `apiVersion: apiextensions.k8s.io/v1beta1` (and not v1)
# until all clusters using this are >= v1.16.0 .
