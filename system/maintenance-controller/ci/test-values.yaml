slack:
  webhook: webhook
  channel: slack_channel
esx:
  enabled: true
  config: some_config
rbac:
  create: true
  serviceAccountName: some-service-account
  cloudProviderSecretName: some-secret
global:
  registry: someregistry.local
config: some_config
kubernikus:
  enabled: true
  config: some_config
priorityClassName: some-class
deployment:
  extraLabels:
    a: b
    c: d
  extraAnnotations:
    e: f
    g: h
ingress:
  enabled: true
