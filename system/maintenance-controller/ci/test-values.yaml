slack:
  webhook: webhook
  channel: slack_channel
esx:
  enabled: true
  config: some_config
global:
  registry: someregistry.local
config: some_config
kubernikus:
  enabled: true
  config: some_config
  cloud-config: cloud-config
ingress:
  enabled: true
