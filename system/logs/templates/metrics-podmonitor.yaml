apiVersion: monitoring.coreos.com/v1
kind: PodMonitor

metadata:
  name: logs-fluent-bit-systemd-exporter
  labels:
    prometheus: {{ .Values.fluent-bit.exporter.prometheus }}

spec:
  jobLabel: logs-fluent-bit-systemd-exporter

  selector:
    matchLabels:
      app.kubernetes.io/name: fluent-bit

  # Find pods in any namespace.
  namespaceSelector:
    matchNames: [logs]

  podMetricsEndpoints:
    - interval: 60s
      scrapeTimeout: 50s
      path: /api/v1/metrics/prometheus
      scheme: http
      port: http
      relabelings:
        - targetLabel: job
          replacement: logs-fluent-bit-systemd-exporter
