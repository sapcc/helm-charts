{{- if .Values.ipPoolImport -}}
{{- range $name, $ippools := .Values.ipPoolImport }}
---
apiVersion: argora.cloud.sap/v1alpha1
kind: IPPoolImport
metadata:
  name: {{ $name }}
  namespace: argora-system
spec:
  ippools:
{{ toYaml $ippools | nindent 2 }}
{{- end }}
{{- end }}
