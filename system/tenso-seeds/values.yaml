owner-info:
  support-group: containers
  service: tenso
  maintainers:
    - Stefan Majewsky
    - Sandro Jäckel
  helm-chart-url: https://github.com/sapcc/helm-charts/tree/master/system/tenso-seeds

# NOTE: This chart shares the regional values.yaml file with the `system/tenso` chart.

tenso:
  service_password: null        # provided by regional values.yaml
  concourse_user_password: null # provided by regional values.yaml
  ansible_user_password: null   # provided by regional values.yaml
