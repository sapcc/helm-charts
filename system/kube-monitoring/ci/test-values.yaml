global:
  region: regionOne
  domain: evil.corp

authentication:
  enabled: true
  ssoCert: "valid certificate"
  ssoKey: "valid private key"


extraScrapeConfig: |
  - job_name: 'dummy'

prober-static:
  enabled: false
  alerts:
    enabled: false
