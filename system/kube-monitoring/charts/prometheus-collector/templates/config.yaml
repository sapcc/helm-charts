apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-collector

data:
  {{- $files := .Files }}
  {{ range tuple "aggregation.rules" }}
  {{ . }}: |
{{ $files.Get . | indent 4 }}
  {{- end }}

  prometheus.yaml: |
{{ include "prometheus_yaml" . | indent 4 }}
