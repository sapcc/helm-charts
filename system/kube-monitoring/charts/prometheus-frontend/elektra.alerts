### Elektra

ALERT ElektraPostgresDatabasesize
  IF pg_database_size_bytes{app="elektra-postgresql"} >= 8589934592.0
  FOR 3m
  LABELS {
    service = "elektra",
    severity = "warning",
    context = "elektra",
    dashboard = "elektra-postgres-capacity"
  }
  ANNOTATIONS {
    summary = "Elektra Database size too large",
    description = "The database size for Elektra >= 8 Gb : {{ $labels.postgres_database_size_bytes }} bytes."
  }

ALERT ElektraPumaRequestBacklog
  IF sum(puma_request_backlog{name="elektra"}) > 1.0
  FOR 3m
  LABELS {
    service = "elektra",
    severity = "warning",
    context = "elektra",
    dashboard = "elektra-details"
  }
  ANNOTATIONS {
    summary = "Elektra puma waiting requests",
    description = "Number of puma waiting requests for Elektra > 0 : {{ $labels.puma_request_backlog }}."
  }
