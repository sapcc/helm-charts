ccauth:
  image:
    repository: repository
    version: version
  
  keystone:
    # host:
    adminUsername: admin
    adminPassword: password
    adminDomain: domain
    adminProject: project

  ldap:
    # host: 
    bindPW: bindPW
    bindDN: bindDN
    baseDN: baseDN

global:
  region: region