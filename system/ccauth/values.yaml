image:
  repository: keppel.global.cloud.sap/ccloud/ccauth
  version: "20240806001552"

keystone:
  # host: calculated if not provided
  adminUsername: admin
  # adminPassword: # from secrets
  adminDomain: Default
  adminProject: admin

ldap:
  # host: # calculated if not provided
  # bindPW: # from secrets
  bindDN: CN=monsoon-service,CN=Users,DC=ad,DC=global,DC=cloud,DC=sap
  baseDN: OU=Identities,DC=ad,DC=global,DC=cloud,DC=sap

# global:
   # region: ap-au-1
