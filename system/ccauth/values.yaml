ccauth:
  image:
    repository: hub.global.cloud.sap/monsoon/ccauth
    version: 177403fc3250fe4b6ae8350cc19068cc3bce5bb4
  
  keystone:
    # host: calculated if not provided 
    adminUsername: admin
    # adminPassword: # from secrets
    adminDomain: Default
    adminProject: admin

  ldap:
    # host: # calculated if not provided 
    # bindPW: # from secrets
    bindDN: CN=monsoon-service,CN=Users,DC=ad,DC=global,DC=cloud,DC=sap
    baseDN: OU=Identities,DC=ad,DC=global,DC=cloud,DC=sap

# global:
   # region: ap-au-1