apiVersion: v1
kind: Service
metadata:
  name: datahubdb
  namespace: datahub
  labels:
    app: datahubdb
    name: datahubdb
spec:
  clusterIP: None
  selector:
    app: datahubdb
  ports:
  - name: mysql
    port: 3306
    protocol: TCP
