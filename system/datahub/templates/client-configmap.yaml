apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-client
  labels:
    app: mariadb
    component: database
data:
  .my.cnf: |
    [client]
    port     = 3306
    socket   = /var/run/mysqld/mysqld.sock
    password = {{ .Values.mariadb.root_password }}
