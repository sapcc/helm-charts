apiVersion: extensions/v1beta1
kind: Ingress

metadata:
  name: datahubdb
  namespace: datahub
  annotations:
    kubernetes.io/tls-acme: "true"

spec:
  tls:
     - secretName: datahubdb
       hosts: [{{.Values.db.endpoint_host_public}}.{{.Values.global.region}}.{{.Values.global.tld}}]
  rules:
    - host: {{.Values.db.endpoint_host_public}}.{{.Values.global.region}}.{{.Values.global.tld}}
      http:
        paths:
        - path: /
          backend:
            serviceName: {{.Values.db.endpoint_host_public}}
            servicePort: {{.Values.mariadb.port_public}}
