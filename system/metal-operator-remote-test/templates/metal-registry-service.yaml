apiVersion: v1
kind: Service
metadata:
  name: {{ include "metal-operator-remote-test.fullname" . }}-metal-registry-service
  labels:
  {{- include "metal-operator-remote-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metalRegistryService.type }}
  selector:
    control-plane: controller-manager
    app.kubernetes.io/instance: metal-operator-remote-test
    app.kubernetes.io/name: metal-operator-remote-test
  ports:
  {{- .Values.metalRegistryService.ports | toYaml | nindent 2 }}