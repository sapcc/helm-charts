kind: Service
apiVersion: v1

metadata:
  name: hubcopter-api

spec:
  selector:
    name: hubcopter-api
  ports:
    - port: 8080

---
kind: Service
apiVersion: v1

metadata:
  name: glas-api

spec:
  selector:
    name: glas-api
  ports:
    - port: 8081

