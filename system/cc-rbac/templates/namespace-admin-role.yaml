# This role is meant for namespace admins
# It is supposed to be assigned via role binding on a per namespace level
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cc:admin
rules:
- apiGroups:
  - '*'
  resources:
  - '*'
  verbs:
  - '*'
