apiVersion: v1
kind: Service
metadata:
  name: secrets-injector
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: secrets-injector
  ports:
    - protocol: TCP
      port: 9443
      targetPort: 9443
