{{- range $cluster := .Values.clusters -}}
{{- range $region := $cluster.regions -}}
apiVersion: v1
kind: ConfigMap
metadata:
    name: authentication-config-wl-{{$cluster.name}}-{{$region}}
data:
  config.yaml: |
{{- toYaml $.Values.authenticationConfig | nindent 4 }}
{{- end }}
{{- end }}