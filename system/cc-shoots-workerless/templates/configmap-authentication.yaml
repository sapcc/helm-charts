apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "authentication-config-%s" (include "cc-shoots-workerless.clusterType" .) }}
data:
  config.yaml: |
{{- toYaml $.Values.authenticationConfig | nindent 4 }}
