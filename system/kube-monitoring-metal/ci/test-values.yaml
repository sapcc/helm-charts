global:
  region: regionOne
  domain: evil.corp
  ghcrIoMirror: evil.corp/ghcr-io-mirror

authentication:
  enabled: true
  ssoCert: "valid certificate"
  ssoKey: "valid private key"

grafana:
  admin:
    username: admin
    password: topSecret!

extraScrapeConfig: |
  - job_name: 'dummy'

prober-static:
  enabled: false
  alerts:
    enabled: false

prometheus-server:
  name: ci-test-name
