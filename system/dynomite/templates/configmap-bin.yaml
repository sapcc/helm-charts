apiVersion: v1
kind: ConfigMap
metadata:
  name: bin
  labels:
    app: bin
    release: "{{ .Release.Name }}"
data:
  warmup.sh: |
{{ .Files.Get "bin/warmup.sh" | indent 4 }}
  dynomite_readiness.sh: |
{{ .Files.Get "bin/dynomite_readiness.sh" | indent 4 }}
  dynomite_start.sh: |
{{ .Files.Get "bin/dynomite_start.sh" | indent 4 }}
