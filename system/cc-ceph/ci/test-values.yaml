global:
  cluster: nase
ceph:
  registry: "test"
  image: 
  version: "123"

objectstore:
  enabled: true
  gateway:
    dnsNames: ["foo.bar"]
  service:
    selector:
      app: test
      rook_cluster: test
  name: objectstore
