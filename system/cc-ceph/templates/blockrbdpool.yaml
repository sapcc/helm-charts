{{- if .Values.blockrbdpool.enabled }}
---
apiVersion: ceph.rook.io/v1
kind: CephBlockPool
metadata:
  name: {{ .Values.blockrbdpool.name }}
spec:
  {{- if .Values.blockrbdpool.failureDomain }}
  failureDomain: {{ .Values.blockrbdpool.failureDomain | default "host"  }}
  {{- end }}
  replicated:
    size: {{ .Values.blockrbdpool.size | default 3 }}
    {{- if .Values.blockrbdpool.replicasPerFailureDomain }}
    replicasPerFailureDomain: {{ .Values.blockrbdpool.replicasPerFailureDomain | default 1 }}
    {{- end }}
    {{- if .Values.blockrbdpool.subFailureDomain }}
    subFailureDomain: {{ .Values.blockrbdpool.subFailureDomain | default "host" }}
    {{- end }}
  {{- if .Values.blockrbdpool.crushRoot }}
  crushRoot: {{ .Values.blockrbdpool.crushRoot | default "default" }} 
  {{- end }}
  deviceClass: nvme
  application: rbd
  parameters:
    pg_num: "1024"
    pgp_num: "1024"
    nodelete: {{ $.Values.pool.nodelete | quote }}
    nosizechange: {{ $.Values.pool.nosizechange | quote }}
{{- end }}
