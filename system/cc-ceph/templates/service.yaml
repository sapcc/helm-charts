apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.objectstore.service.name }}
  namespace: {{ .Release.Namespace }}
{{- if or .Values.objectstore.service.annotations .Values.objectstore.service.calicoExternalIP }}
  annotations:
{{- if .Values.objectstore.service.calicoExternalIP }}
    projectcalico.org/loadBalancerIPs: '["{{ .Values.objectstore.service.calicoExternalIP }}"]'
{{- end }}
{{- if .Values.objectstore.service.annotations }}
{{ toYaml .Values.objectstore.service.annotations | indent 4 }}
{{- end }}
{{- end }}
spec:
{{- if .Values.objectstore.service.externalIP }}
  externalIPs:
  - {{ .Values.objectstore.service.externalIP }}
{{- end }}
  type: {{ default "NodePort" .Values.objectstore.service.type }}
  externalTrafficPolicy: Local
  sessionAffinity: None
  ports:
    - port: {{ .Values.objectstore.service.port }}
      targetPort: {{ .Values.objectstore.service.targetPort }}
      protocol: TCP
      name: rgw-ssl
  selector:
    app: {{ .Values.objectstore.service.selector.app }}
    rook_cluster: {{ .Values.objectstore.service.selector.rook_cluster }}
    rook_object_store: {{ .Values.objectstore.name }}
