{{- if .Values.multicloudtest.enabled }}
apiVersion: v1 
kind: ConfigMap
data:
  creds.json: |
{{ toPrettyJson .Values.gcp.creds.json }}
metadata:
  name: multicloudtest-creds-gcs
  namespace: cc3test
{{- end }}
