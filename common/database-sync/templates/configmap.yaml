---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "database-sync.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "database-sync.labels" (list $ "noversion" "database-sync" "cm" "config") | nindent 4 }}
data:
  config.yaml: |
{{ include "database-sync.configmap" . | indent 4 }}
