schema:
  type: object
  properties:
    patterns:
      type: array
      items:
        description: regex pattern
        type: string
      minItems: 0

testValues:
  patterns: [ bogus-pattern-for-testing ]
