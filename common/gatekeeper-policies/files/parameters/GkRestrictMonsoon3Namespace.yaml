schema:
  type: object
  properties:
    patterns:
      type: object
      properties:
        monsoon3:
          type: string
        monsoon3global:
          type: string

testValues:
  patterns:
    monsoon3: "^(?:foo|bar)$"
    monsoon3global: "^(?:foo-global|bar-global)$"
