apiVersion: v2
name: rendered-chart
description: |
  This chart renders every template and constraint from the gatekeeper-policies chart for testing purposes.
  We cannot put the templating within this chart into the gatekeeper-policies chart because its functions only work when it is imported as a subchart.
version: 1.0.0
dependencies:
  - name: gatekeeper-policies
    repository: file://../..
    version: '>= 0.0.0'
