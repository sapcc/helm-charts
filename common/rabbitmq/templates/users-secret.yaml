apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-users
  labels:
    app: {{ template "fullname" . }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    component: rabbitmq
data:
  users.yaml: |
    {{ include (print .Template.BasePath "/etc/_users-secret.tpl") . | b64enc | indent 4 }}
