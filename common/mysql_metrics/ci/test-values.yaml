---
global:
  registry: test

test_db_host: testRelease-mariadb.svc

db_password: secret!
db_name: myDB
db_user: testUser

connections:
  default:
    db_type: mariadb
    db_instance_name: null
    db_name: defaultDb
    db_user: userDefault
    db_password: passwordDefault
  custom:
    db_type: mariadb
    db_instance_name: custom-b-1
    db_name: customDb
    db_user: userCustom
    db_password: passwordCustom
