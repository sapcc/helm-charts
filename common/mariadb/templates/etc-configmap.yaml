apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-etc
  labels:
    app: mariadb
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    component: database
data:
  mariadb.cnf: |
{{ include (print .Template.BasePath "/etc/_mariadb.cnf.tpl") . | indent 4 }}
