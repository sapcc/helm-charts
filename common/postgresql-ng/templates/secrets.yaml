apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
type: Opaque
data:
  postgres-password:  {{ .Values.postgresPassword | required "missing value for .Values.postgresPassword" | b64enc | quote }}
