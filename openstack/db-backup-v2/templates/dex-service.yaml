apiVersion: v1
kind: Service
metadata:
  name: db-backup-dex
  namespace: monsoon3
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
  selector:
    app: db-backup-dex