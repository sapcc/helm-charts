apiVersion: v1
kind: ConfigMap
metadata:
  name: ceilometer-etc
  labels:
    system: openstack
    service: metering
    component: configuration

data:
  api-paste.ini: |
{{ include "api_paste_ini_tpl" . | indent 4 }}
  ceilometer.conf: |
{{ include "ceilometer_conf_tpl" . | indent 4 }}
  event-definitions.yaml: |
{{ include "event_definitions_yaml_tpl" . | indent 4 }}
  event-pipeline.yaml: |
{{ include "event_pipeline_yaml_tpl" . | indent 4 }}
  pipeline.yaml: |
{{ include "pipeline_yaml_tpl" . | indent 4 }}
  policy.json: |
{{ include "policy_json_tpl" . | indent 4 }}
  healthcheck.conf: |
{{ include "healthcheck_conf_tpl" . | indent 4 }}
