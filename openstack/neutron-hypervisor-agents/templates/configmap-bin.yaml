apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-bin
  labels:
    system: openstack
    type: configuration
    component: neutron
data:
  start-ovsdb-server: |
{{ include (print .Template.BasePath "/bin/_start-ovsdb-server.tpl") . | indent 4 }}
  start-ovs-vswitchd: |
{{ include (print .Template.BasePath "/bin/_start-ovs-vswitchd.tpl") . | indent 4 }}
{{ (.Files.Glob "bin/*").AsConfig | indent 2 }}
