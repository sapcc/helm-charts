apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-etc-linuxbridge
  labels:
    system: openstack
    type: configuration
    component: neutron

data:
  neutron-linuxbridge-agent.conf: |
{{ include (print .Template.BasePath "/etc/_neutron-linuxbridge.conf.tpl") . | indent 4 }}
