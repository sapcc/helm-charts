apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
data:
  username: {{ .Values.username | required "username required" | b64enc }}
  password: {{ .Values.password | required "password required" | b64enc }}
