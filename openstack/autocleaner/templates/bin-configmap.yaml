apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-bin
  labels:
    system: openstack
    type: configuration
    component: autocleaner
data:
{{ (.Files.Glob "bin/*").AsConfig | indent 2 }}
