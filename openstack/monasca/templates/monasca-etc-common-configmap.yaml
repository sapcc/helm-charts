---
apiVersion: v1
kind: ConfigMap
metadata:
  name: monasca-etc-common
  labels:
    system: openstack
    service: monitoring
    component: configuration

data:
  kafka-cluster-server.properties: |
{{ include "monasca_kafka_cluster_server_properties_tpl" . | indent 4 }}
  kafka-cluster-consumer.properties: |
{{ include "monasca_kafka_cluster_consumer_properties_tpl" . | indent 4 }}
  kafka-cluster-zookeeper.properties: |
{{ include "monasca_kafka_cluster_zookeeper_properties_tpl" . | indent 4 }}
  kafka-cluster-log4j.properties: |
{{ include "monasca_kafka_cluster_log4j_properties_tpl" . | indent 4 }}
  kafka-manager-application.conf: |
{{ include "monasca_kafka_manager_application_conf_tpl" . | indent 4 }}
