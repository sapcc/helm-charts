{{- if .Values.vcenter_exporter.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: vcenter-exporter-etc
  labels:
    system: openstack
    service: monitoring
    component: configuration

data:
  config.yaml: |
{{ include "monasca/templates/etc/vcenter-exporter/_config.yaml.tpl" . | indent 4 }}
{{- end }}
