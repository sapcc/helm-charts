apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: monasca-logstash-persister
  namespace: monasca
  labels:
    system: openstack
    service: monitoring
  annotations:
    kubernetes.io/change-cause: {{.Values.cluster_deployment_reason}}

spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        component: monasca-logstash-persister
      annotations:
        checksum/monasca.etc.log: checksums#monasca-etc-log-configmap.yaml#
    spec:
      nodeSelector:
        zone: farm
      volumes:
        - name: openstack-kube
        - name: monasca-etc-log
          configMap:
            name: monasca-etc-log
      containers:
        - name: monasca-logstash-persister
          image: {{.Values.docker_repo}}/monasca-logstash:{{.Values.image_version_monasca_logstash}}
          imagePullPolicy: IfNotPresent
          command:
            - /logstash/bin/logstash
          args:
            - -f
            - /monasca-etc-log/logstash-persister-monasca-log-persister.conf
          volumeMounts:
            - name: monasca-etc-log
              mountPath: /monasca-etc-log
