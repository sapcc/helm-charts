Archer {{ .Values.image.tag }} has been deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Endpoints:
{{- if .Values.ingress.enabled }}
  - Public:   http{{ if .Values.ingress.tls }}s{{ end }}://{{ include "archer.fullname" . }}.{{ .Values.global.region }}.{{ .Values.global.tld }}/
{{- end }}
  - Cluster:  http://{{ include "archer.fullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDNSSearchDomain | default "cluster.local" }}:{{ .Values.service.port }}

Keystone seed:
  The OpenstackSeed for archer is applied in the "{{ required ".Values.global.keystoneNamespace required" .Values.global.keystoneNamespace }}" namespace
  and depends on "{{ .Values.global.keystoneNamespace }}/keystone-seed".

Useful commands:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/name={{ include "archer.name" . }},app.kubernetes.io/component=api -f
  kubectl -n {{ .Release.Namespace }} get job -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if not .Values.serviceAccount.create }}

NOTE: serviceAccount.create is false. Pods will run under the "{{ include "archer.serviceAccountName" . }}" service account
      in namespace "{{ .Release.Namespace }}" -- make sure it exists and has the RBAC needed by kubernetes-entrypoint
      (get/list on pods, services, endpoints, jobs, daemonsets).
{{- end }}
