apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: blackbox-api
  namespace: blackbox

data:
  os.password: {{ default "" .Values.os_password | b64enc | quote }}
  swift.api.key: {{ default "" .Values.swift_api_key | b64enc | quote }}