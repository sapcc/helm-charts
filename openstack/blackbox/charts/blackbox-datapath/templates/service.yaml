kind: Service
apiVersion: v1

metadata:
  name: blackbox-api
  namespace: blackbox
  annotations:
    "prometheus.io/scrape": "true"

spec:
  selector:
    name: blackbox-api
  ports:
    - port: 9101