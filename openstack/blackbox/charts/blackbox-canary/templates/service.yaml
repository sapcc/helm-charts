kind: Service
apiVersion: v1

metadata:
  name: healthcheck-canary
  namespace: monitoring
  annotations:
    "prometheus.io/scrape": "true"

spec:
  selector:
    name: canary
  ports:
    - port: 9101