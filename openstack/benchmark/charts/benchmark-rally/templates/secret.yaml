apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: benchmark-rally

data:
  rally.admin.user.password: {{ default "" .Values.rally_admin_user_password | b64enc | quote }}
  rally.test.user.password: {{ default "" .Values.rally_test_user_password | b64enc | quote }}