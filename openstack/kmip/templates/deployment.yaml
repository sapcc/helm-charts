apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kmip.fullname" . }}-barbican
  labels:
    {{- include "kmip.labels" . | nindent 4 }}
    app: {{ include "kmip.fullname" . }}-barbican
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    system: openstack
    type: api
    component: barbican
spec:
  replicas: {{ .Values.replicaCount }}

  revisionHistoryLimit: {{ .Values.pod.lifecycle.upgrades.deployments.revisionHistory }}
  strategy:
    type: {{ .Values.pod.lifecycle.upgrades.deployments.podReplacementStrategy }}
    {{- if eq .Values.pod.lifecycle.upgrades.deployments.podReplacementStrategy "RollingUpdate" }}
    rollingUpdate:
      maxUnavailable: {{ .Values.pod.lifecycle.upgrades.deployments.rollingUpdate.maxUnavailable }}
      maxSurge: {{ .Values.pod.lifecycle.upgrades.deployments.rollingUpdate.maxSurge }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "kmip.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kmip.labels" . | nindent 8 }}
        app: {{ include "kmip.fullname" . }}-barbican
        name: {{ include "kmip.fullname" . }}-barbican
        alert-tier: os
        alert-service: barbican
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        configmap-etc-hash: {{ include (print $.Template.BasePath "/etc-configmap.yaml") . | sha256sum }}
        {{- if .Values.proxysql.mode }}
        prometheus.io/scrape: "true"
        prometheus.io/targets: {{ required ".Values.alerts.prometheus missing" .Values.alerts.prometheus | include "resolve_secret" }}
        {{- end }}        
    spec:
      serviceAccountName: {{ include "kmip.serviceAccountName" . }}-barbican
      containers:
        - name: kmip-restapi
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{required ".Values.global.registry is missing" .Values.global.registry }}/loci-barbican:{{required "Values.kmip.restapi_image is missing" .Values.kmip.restapi_image }}
          imagePullPolicy: {{ .Values.kmip.pullPolicy }}
          env:
            - name: KMIP_MARIADB_SERVICE_HOST
              value: "kmip-mariadb"
            - name: KMIP_MARIADB_SERVICE_PORT
              value: "3306"
            - name: KMIP_MARIADB_SERVICE_USER
              value: {{ .Values.mariadb.users.kmip.user | include "resolve_secret" }}
            - name: KMIP_MARIADB_SERVICE_PASSWORD
              value: {{ .Values.mariadb.users.kmip.password | include "resolve_secret" }}
            - name: KMIP_MARIADB_NAME
              value: "kmip"
            - name: BARBICAN_MARIADB_SERVICE_HOST
              value: "barbican-mariadb"
            - name: BARBICAN_MARIADB_SERVICE_PORT
              value: "3306"
            - name: BARBICAN_MARIADB_SERVICE_USER
              value: {{ .Values.mariadb.users.barbican.user | include "resolve_secret" }}
            - name: BARBICAN_MARIADB_SERVICE_PASSWORD
              value: {{ .Values.mariadb.users.barbican.password | include "resolve_secret" }}
            - name: BARBICAN_MARIADB_NAME
              value: "barbican"
          command: ["sh", "-c"]
          args:
          - |
            echo "Sleeping for 15 seconds before starting the application...";
            sleep 15;
            echo "Starting the application...";
            python /app/app.py;
            echo "Application exited. Keeping container alive...";
            tail -f /dev/null
          resources:
            requests:
              memory: 0
              cpu: 0
        - name: kmip-core
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{required ".Values.global.registry is missing" .Values.global.registry }}/loci-barbican:{{required "Values.kmip.image is missing" .Values.kmip.image }}
          imagePullPolicy: {{ .Values.kmip.pullPolicy }}
          env:
            - name: OS_AUTH_URL
              value: "https://{{ include "keystone_api_endpoint_host_public" . }}:443/v3"
            - name: OS_USERNAME
              value: {{ .Values.kmip.openstack_env.username | include "resolve_secret" }}
            - name: OS_PASSWORD
              value: {{ .Values.kmip.openstack_env.password | include "resolve_secret" }}
            - name: OS_PROJECT_ID
              value: {{ .Values.kmip.openstack_env.project_id | include "resolve_secret" }}
            - name: OS_APPLICATION_CREDENTIAL_NAME
              value: {{ .Values.kmip.openstack_env.appl_cred_name | include "resolve_secret" }}
            - name: OS_APPLICATION_CREDENTIAL_SECRET
              value: {{ .Values.kmip.openstack_env.appl_cred_secret | include "resolve_secret" }}
            - name: OS_AUTH_TYPE
              value: "v3applicationcredential"
            - name: OS_REGION_NAME
              value: {{ .Values.kmip.openstack_env.region_name | include "resolve_secret" }}
            - name: OS_PROJECT_DOMAIN_NAME
              value: {{ .Values.kmip.openstack_env.project_domain_name | include "resolve_secret" }}
            - name: OS_IDENTITY_API_VERSION
              value: {{ .Values.kmip.openstack_env.identity_api_version | include "resolve_secret" }}
            - name: OS_PROJECT_NAME
              value: {{ .Values.kmip.openstack_env.project_name | include "resolve_secret" }}
            - name: OS_USER_DOMAIN_NAME
              value: {{ .Values.kmip.openstack_env.user_domain_name | include "resolve_secret" }}
          ports:
            - name: http
              containerPort: {{ .Values.service.ports.port }}
              protocol: TCP           
          resources:
            requests:
              memory: 0
              cpu: 0
          volumeMounts:
            - name: kmip-barbican-etc
              mountPath: /etc/pykmip/server.conf
              subPath: kmip-server.conf
              readOnly: true
            - name: kmip-certificates
              mountPath: /etc/pykmip/certs/server.crt
              subPath: tls.crt
              readOnly: true
            - name: kmip-certificates
              mountPath: /etc/pykmip/certs/server.key
              subPath: tls.key
              readOnly: true
            - name: kmip-certificates
              mountPath: /etc/pykmip/certs/ca.crt
              subPath: ca.crt
              readOnly: true
      volumes:
        - name: kmip-barbican-etc
          configMap:
            name: kmip-barbican-etc
        - name: kmip-certificates
          secret:
            secretName: kmip-certificates
