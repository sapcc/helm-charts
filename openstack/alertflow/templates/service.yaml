{{- if .Values.alertflow.enabled }}

apiVersion: v1
kind: Service
metadata:
  name:  alertflow
  namespace: {{ .Values.namespace }}
  labels:
spec:
  selector:
  ports:
  - name: http
    port: {{ .Values.alertflow.port }}
    protocol: TCP

{{- end }}
