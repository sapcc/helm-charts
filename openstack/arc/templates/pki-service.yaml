apiVersion: v1
kind: Service
metadata:
  name: arc-pki
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  type: {{ .Values.pkiService.type }}
  ports:
  - port: {{ .Values.pkiService.externalPort }}
    targetPort: {{ .Values.pkiService.internalPort }}
    protocol: TCP
    name: {{ .Values.pkiService.name }}
  selector:
    app: arc-pki 
