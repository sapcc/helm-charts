# Umbrella Helm Chart for KVM HA Monitor Ping

This Helm chart deploys the KVM HA Monitor Ping, which monitors KVM hypervisor reachability via ICMP.
It is an umbrella chart with the public [kvm-ha-monitor-ping](https://github.com/cobaltcore-dev/kvm-ha-monitor-ping) chart as a dependency to meet internal configuration needs.
