# Umbrella Helm Chart for KVM HA Agent

This Helm chart deploys the KVM HA Agent, which is responsible for monitoring KVM virtual machines.
It is an Umbrella chart with the public [kvm-ha-agent](https://github.com/cobaltcore-dev/kvm-ha-agent) chart as a dependency to meet internal configuration needs.
