global:
  keystoneNamespace: keystone
  region: xx-yy-1
  tld: example.org
  dockerHubMirror: registry.example.com/dockerhub
  dockerHubMirrorAlternateRegion: registry-other.example.com/dockerhub
  registry: registry.example.com
  registryAlternateRegion: registry-other.example.com
  quayIoMirror: registry.example.com/quayio
  vaultBaseURL: vault.example.com/secrets

  domain_seeds:
    customer_domains: [ bar, foo, baz ]
    customer_domains_without_support_projects: [ baz ]

keppel:
  image_tag: '12345'

  peer_group: test
  peer_groups:
    test:
      leader: xx-yy-1
      members: [ xx-yy-1, xx-yy-2 ]
      anycast_domain_name: registry-xx-yy.example.com
