apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.netapp.secret.name }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.netapp.secret.username }}
  password: {{ .Values.netapp.secret.password }}