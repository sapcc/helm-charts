
global:
  region: region_one
netapp:
  filerexpr: ^testfiler-tf13.*
  filers: []
  volumeregextemplate: "(^cinder_|^nfs_{{FilerName}}_).*"
  secret:
    name: 'kvm-cinder-netapp-operator-secret'
    username: username
    password: password
