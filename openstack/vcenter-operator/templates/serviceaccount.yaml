apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
{{ include "helm3-helper.annotations" . | indent 4 }}
  labels:
{{ include "helm3-helper.labels" . | indent 4 }}
  name: {{ .Chart.Name }}
