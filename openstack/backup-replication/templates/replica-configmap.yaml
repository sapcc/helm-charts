kind: ConfigMap
apiVersion: v1
metadata:
  name: backup-replica-config
data:
  from.env: |
{{ include "from.env" .Values.from | indent 4 }}
{{- range $key, $val := .Values.to }}
  to{{- $key -}}.env: |
{{ include "to.env" $val | indent 4 }}
{{- end }}
