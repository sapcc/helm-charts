global:
  dbPassword: topSecret
  dbBackupServicePassword: topSecret
  netapp:
    filers:
      - name: topSecret
        host: topSecret
        protocol: topSecret
        port: topSecret
        username: topSecret
        password: topSecret
        root_volume_aggregate: topSecret
        aggregate_search_pattern: topSecret
        port_search_pattern: topSecret
        physical_network: topSecret
        mtu: topSecret
  imageRegistry: myRegistry

loci:
  imageVersion: stein

mariadb:
  root_password: topSecret

mysql_metrics:
  db_password: topSecret
