#!/usr/bin/env bash

set -e
set -x

function start_unit_tests {
    sudo apt-get install -y build-essential python-dev python3-dev
    cd manila && sudo ./run_tests.sh -V --concurrency 0
}

start_unit_tests
