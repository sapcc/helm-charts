#!/usr/bin/env bash

set -ex

#
# to make agents named after the node
#
echo "[DEFAULT]" > /etc/manila/hostname.conf
echo "host = $NODE_NAME" >> /etc/manila/hostname.conf

exec manila-scheduler --config-file /etc/manila/manila.conf --config-file /etc/manila/hostname.conf
