{{- $envAll := . }}
---
{{ tuple $envAll "cell1" (include "cell1_transport_url" .) (include "nova.helpers.db_url" (tuple . "cell1")) | include "nova.cell_secret" }}
{{- if .Values.cell2.enabled }}
---
{{ tuple $envAll .Values.cell2.name (include "cell2_transport_url" .) (include "nova.helpers.db_url" (tuple . "cell2")) | include "nova.cell_secret" }}
{{- end }}
