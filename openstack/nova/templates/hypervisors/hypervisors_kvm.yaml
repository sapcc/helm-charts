{{- $envAll := . }}
{{- range $i, $hypervisor := .Values.hypervisors_kvm }}
  {{- if ne $i 0 }}
---
  {{- end }}
  {{- tuple $envAll $hypervisor | include "kvm_hypervisor" }}
---
  {{- tuple $envAll $hypervisor | include "kvm_configmap" }}
{{- end }}
