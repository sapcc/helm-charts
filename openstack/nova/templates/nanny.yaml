{{/* NOTE: Each nanny requires a script to execute with corresponding filename in templates/bin/nanny */}}
{{- range $name := list "db-archive-deleted-rows" "db-purge" "db-soft-delete-excessive-instance-faults" "sap-map-instances"}}
{{ include "nova.nanny_cronjob" (tuple $ $name) }}
{{- end }}
