kind: Ingress
apiVersion: networking.k8s.io/v1

metadata:
  name: aurora
  annotations:
    kubernetes.io/tls-acme: "true"
    # create dns record for dashboard
    dns-manager: "true"
    dns-manager/zone-name: "dashboard-aurora.{{.Values.global.region}}.{{ .Values.global.tld }}"
spec:
  tls:
    - secretName: aurora-ingress-tls
      # prettier-ignore
      hosts: ["dashboard-aurora.{{.Values.global.region}}.{{ .Values.global.tld }}"]
  rules:
    - host: "dashboard-aurora.{{.Values.global.region}}.{{ .Values.global.tld }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aurora
                port:
                  number: 3000
