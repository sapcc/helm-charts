apiVersion: v1
kind: Secret
metadata:
  name: maillog
  namespace: {{ .Release.Namespace }}
  labels:
    app: maillog
    chart: maillog
    release: maillog
    heritage: {{ .Release.Service }}
type: Opaque
data:
  config.yaml: {{ include "maillog-config" . | b64enc }}
