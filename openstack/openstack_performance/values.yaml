# Default values for locust.
# This is a YAML-formatted file.

enabled: true

image:
  repository: keppel.eu-de-1.cloud.sap/locustio/locust
  pullPolicy: Always

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
