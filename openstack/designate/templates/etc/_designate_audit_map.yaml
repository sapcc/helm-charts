service_type: 'service/dns'
service_name: 'designate'

prefix: '/v2'

resources:
  zones:
    children:
      recordsets:
      
  pools:
  limits:
    singleton: true
  # Default mappings
  tlds:
  tsigkeys:
  blacklists:
  quotas:
    singleton: true

  service_statuses:
    service_name: name

  floatingips:
