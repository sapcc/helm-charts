service_type: 'service/dns'
service_name: 'designate'

prefix: '/v2'

resources:
  zones:
    children:
      recordsets:

  #Get with Pool ID
  pools:
  # A single Get, does it get added? 
  #limits:
  #  singleton: true

  # Tld operations, Post + get/patch/delete with ID
  tlds:

  #
  tsigkeys:
  blacklists:
  quotas:
  # This is a single get, so likely not added.
  #service_statuses:


  floatingips:
