---
apiVersion: v1
kind: ConfigMap
metadata:
  name: logstash-etc
  labels:
    system: openstack
    service: logging
    component: configuration

data:
  logstash.conf: |
{{ include "elk/templates/etc/_logstash.conf.tpl" . | indent 4 }}
  audit.json: |
{{ include "elk/templates/etc/_netflow.json.tpl" . | indent 4 }}
  logstash.yaml: |
{{ include "elk/templates/etc/_logstash.yaml.tpl" . | indent 4 }}
