---
apiVersion: v1
kind: ConfigMap
metadata:
  name: es-bin
  labels:
    system: openstack
    service: logging
    component: configuration

data:
  elasticsearch-start: |
{{ include "elk/templates/bin/_elasticsearch-start.tpl" . | indent 4 }}
  es-enable-sync.sh: |
{{ include "elk/templates/bin/_es-enable-sync.sh.tpl" . | indent 4 }}
  es-disable-sync.sh: |
{{ include "elk/templates/bin/_es-disable-sync.sh.tpl" . | indent 4 }}
