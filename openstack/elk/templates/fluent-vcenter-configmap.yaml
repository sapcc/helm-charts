apiVersion: v1
kind: Service

metadata:
  name: fluent-vcenter
  labels:
    system: openstack
    service: logging
    component: configuration

data:
  fluentd.conf: |
{{ include "elk/templates/etc/_fluent_vcenter.conf.tpl" . | indent 4 }}
