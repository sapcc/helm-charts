apiVersion: v1
kind: ConfigMap
metadata:
  name: logstash-etc
  labels:
    system: openstack
    service: logging
    component: configuration

data:
  prometheus_port: 24231,
  syslog_port: 5410,
  fluentd.conf: |
{{ include "elk/templates/etc/_logstash.conf.tpl" . | indent 4 }}