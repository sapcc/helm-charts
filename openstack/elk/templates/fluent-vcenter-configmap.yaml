apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-vcenter-etc
  labels:
    system: openstack
    service: logging
    component: configuration

data:
  prometheus_port: {{.Values.fluent_vcenter.prometheus_port}},
  syslog_port: {{.Values.fluent_vcenter.tcp_input_port}},
  fluentd.conf: |
{{ include "elk/templates/etc/_fluent_vcenter.conf.tpl" . | indent 4 }}