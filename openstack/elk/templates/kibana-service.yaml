apiVersion: v1
kind: Service
metadata:
  name: elk-kibana
  namespace: elk
  labels:
    system: openstack
    service: logging
spec:
  selector:
    component: elk-kibana
  ports:
    - port: {{.Values.elk_kibana_port_public}}
