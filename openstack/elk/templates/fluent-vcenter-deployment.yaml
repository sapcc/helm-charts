apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: fluent-vcenter
  namespace: elk
  labels:
    system: openstack
    service: logging

spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        component: fluent-vcenter
      annotations:
        checksum/fluent-vcenter-configmap.yaml: {{ include "elk/templates/fluent-vcenter-configmap.yaml" . | sha256sum }}
    spec:
      nodeSelector:
        zone: farm
      volumes:
        - name: elk-etc
          configMap:
            name: fluent-vcenter
      containers:
        - name: fluent-vcenter
          image: {{.Values.docker_repo}}/fluent-vcenter:{{.Values.fluent_vcenter.image_version}}
          imagePullPolicy: IfNotPresent
          ports:
            - name: syslog
              containerPort: {{.Values.fluent_vcenter.tcp_input_port}}
              protocol: TCP
          env:
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          volumeMounts:
            - name: elk-etc
              mountPath: /etc
