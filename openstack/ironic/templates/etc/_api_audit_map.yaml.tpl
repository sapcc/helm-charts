service_type: 'compute/baremetal'
service_name: 'ironic'
prefix: '/v1'

resources:
  allocations:
  deploy_templates:
  nodes:
    children:
      allocation:
      validate:
      maintenance:
      management:
      states:
      traits:
      vifs:
  ports:
  portgroups:
  volumes:
    children:
      connectors:
      targets:

