service_type: 'compute/baremetal'
service_name: 'ironic'
prefix: '/v1'

resources:
  allocations:
  chassis:
  deploy_templates:
  nodes:
    children:
      allocation:
      validate:
      maintenance:
      management:
      states:
      traits:
      vifs:
      vmedia:
  ports:
  portgroups:
  runbooks:
  volumes:
    children:
      connectors:
      targets:

