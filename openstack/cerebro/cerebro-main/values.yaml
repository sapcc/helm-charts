image:
  pullPolicy: IfNotPresent
  tag: SET-AS-HELM-PARAMETER # This should be set via --set to a git commit hash
  registry: SET-AS-HELM-PARAMETER

vernemq:
  vernemq:
    image:
      repository: SET-AS-HELM-PARAMETER
      tag: SET-AS-HELM-PARAMETER  # This should be set via --set to a git commit hash


migrationRecommenderService:
  prometheusClientPort: 8092

optimizationService:
  prometheusClientPort: 8092

dcStateService:
  prometheusClientPort: 8092
