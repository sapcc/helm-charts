# Helm Chart for OpenStack Elektra

to update the dependencies run:

```bash
helm dependency update
```

This will update all dependent Charts defined in Charts.yaml and will lock the version in Charts.lock
