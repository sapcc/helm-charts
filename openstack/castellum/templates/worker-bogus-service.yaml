kind: Service
apiVersion: v1

# This service is only needed because Maia does not look for annotations on pods.
metadata:
  name: castellum-worker
  annotations:
    maia.io/scrape: "true"
    prometheus.io/port: "8080"
spec:
  selector:
    name: castellum-worker
  ports:
    - port: 8080

