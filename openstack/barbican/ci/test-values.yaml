global:
  pgbouncer:
    imageTag: "20190123.2"
  registry: myImage
  dbPassword: topSecret
  barbican_service_password: topSecret
  domain_seeds:
    skip_hcm_domain: false
    
imageVersionBarbicanApi: train

dbPassword: topSecret
mariadb:
  root_password: topSecret

rabbitmq_notifications:
  users:
    default:
      password: topSecret

api:
  replicas: 2
  resources:
    enabled: false
