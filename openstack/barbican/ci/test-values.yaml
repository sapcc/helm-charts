global:
  pgbouncer:
    imageTag: "20190123.2"
  imageRegistry: myImage
  dbPassword: topSecret

imageVersionBarbicanApi: train

dbPassword: topSecret
mariadb:
  root_password: topSecret

rabbitmq_notifications:
  users:
    default:
      password: topSecret

api:
  repliacs: 2
