apiVersion: v1
kind: ConfigMap
metadata:
  name: barbican-scripts
  labels:
    system: openstack
    type: configuration
    component: barbican

data:
  barbican-db-check.sh: |
{{ include (print .Template.BasePath "/scripts/_move-secrets.sh.tpl") . | indent 4 }}
