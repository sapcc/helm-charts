# Umbrella Helm Chart for KVM HA Service

This Helm chart deploys the KVM HA Service, which is responsible for monitoring KVM virtual machines.
It is an Umbrella chart with the public [kvm-ha-service](https://github.com/cobaltcore-dev/kvm-ha-service) chart as a dependency to meet internal configuration needs.
