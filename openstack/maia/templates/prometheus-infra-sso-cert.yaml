apiVersion: v1
kind: Secret

metadata:
  name: prometheus-infra-sso-cert

data:
  prometheus_infra_sso.crt: {{ .Values.prometheus_infra.ssoCert | b64enc | quote }}
  prometheus_infra_sso.key: {{ .Values.prometheus_infra.ssoKey | b64enc | quote }}