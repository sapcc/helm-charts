apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: prometheus-maia

data:
  tls.crt: {{ default "" .Values.prometheus.tls_crt | b64enc | quote }}
  tls.key: {{ default "" .Values.prometheus.tls_key | b64enc | quote }}
