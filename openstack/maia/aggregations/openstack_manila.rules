groups:
- name: manila
  rules:
  - expr: 0 * openstack_manila_share_total_bytes{volume=~"share.*"} + on(volume) group_left() openstack_manila_share_total_ops{volume=~"share.*"}
    record: openstack_manila_share_total_ops
