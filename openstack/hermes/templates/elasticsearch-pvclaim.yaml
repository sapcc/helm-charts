---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-elasticsearch
spec:
  annotations:
    "helm.sh/resource-policy": keep
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{.Values.hermes_elasticsearch_data_volumesize}}
