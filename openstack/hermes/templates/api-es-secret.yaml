{{- if and .Values.hermes.es_password }}
apiVersion: v1
kind: Secret

metadata:
  name: hermes-es-api
  namespace: hermes
  labels:
    app: hermes-api

data:
  ES_PW: {{ required "missing Hermes Elasticsearch pw" .Values.hermes.es_password | b64enc }}

---
{{- end }}
