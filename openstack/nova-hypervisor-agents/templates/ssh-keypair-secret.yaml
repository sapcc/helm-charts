apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-ssh-keypair
  labels:
    system: openstack
    type: configuration
    component: nova
type: kubernetes.io/ssh-auth
data:
  ssh-privatekey: {{ .Values.ssh.private | b64enc }}
  ssh-publickey: {{ .Values.ssh.public | b64enc }}
  ssh-authorizedkeys: {{ .Values.ssh.public | b64enc }}
