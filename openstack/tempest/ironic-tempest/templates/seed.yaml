apiVersion: "openstack.stable.sap.cc/v1"
kind: "OpenstackSeed"
metadata:
  name: {{ .Chart.Name }}-seed
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:

# TODO: Put your seeding here, otherwise delete the file.

# TODO: If you seed users and projects then spend quota via limes code and the limes pipeline: 
# staging: https://github.wdf.sap.corp/cc/secrets/blob/master/staging/values/quota-constraints.yaml
# qa-de-1: https://github.wdf.sap.corp/cc/secrets/blob/master/qa-de-1/values/quota-constraints.yaml
# limes pipeline: https://ci.global.cloud.sap/teams/main/pipelines/limes