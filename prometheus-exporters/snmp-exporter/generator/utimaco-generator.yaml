modules:
  utimaco:
    walk: [ cslVersion, cslSerialNumber, cslBatteryState, cslLoad, cslClients, cslClientsLoad, cslFanSpeed, cslPowerSupply, cslCPUTemperature, cslPowerSupplyStatus, csDevice, csMode, csState, csTemperature,  csAlarm, csVersion, csSerialNumber, csBatteryState, csModuleState, csTransactionsPerMinute, csarDevice, csarState, csarTemperature, csarAlarm, csarVersion, csarSerialNumber, csarBatteryState, cHSMValid, cHSMLoad, cHSMTransactionsPerMinute, cHSMConnections, ifInOctets, ifInUcastPkts, ifInNUcastPkts, ifInDiscards, ifInErrors, ifInUnknownProtos, ifOutOctets, ifOutUcastPkts, ifOutNUcastPkts, ifOutDiscards, ifOutErrors, ifOutQLen, ipInReceives, ipInHdrErrors, ipInAddrErrors, ipForwDatagrams, ipInUnknownProtos, ipInDiscards, ipInDelivers, ipOutRequests, ipOutDiscards, ipOutNoRoutes, ipReasmTimeout, ipReasmReqds, ipReasmOKs, ipReasmFails, ipFragOKs, ipFragFails, ipFragCreates, ipAdEntAddr, ipAdEntNetMask, ipAdEntBcastAddr, hrStorageSize, hrStorageUsed, hrProcessorLoad, hrSWRunPerfCPU, hrSWRunPerfMem, memTotalReal, memAvailReal, laLoad, ssCpuUser, ssCpuSystem, ssCpuIdle, cHSMDisk, cHSMLoad, cHSMTransactionsPerMinute, cHSMConnections ]
    lookups:
      - source_indexes: [cslPowerSupplyIndex]
        lookup: cslPowerSupplyStatusAsString
      - source_indexes: [csIndex]
        lookup: csDevice
      - source_indexes: [csIndex]
        lookup: csSerialNumber
      - source_indexes: [csIndex]
        lookup: csVersion
      - source_indexes: [cHSMIndex]
        lookup: cHSMDevice
      - source_indexes: [cHSMIndex]
        lookup: cHSMMode
      - source_indexes: [cHSMIndex]
        lookup: cHSMState
      - source_indexes: [cHSMIndex]
        lookup: cHSMTemplate
      - source_indexes: [cHSMIndex]
        lookup: cHSMModuleState
      - source_indexes: [ifIndex]
        lookup: ifDescr
      - source_indexes: [ifIndex]
        lookup: ifType
      - source_indexes: [ifIndex]
        lookup: ifMtu
      - source_indexes: [ifIndex]
        lookup: ifSpeed
      - source_indexes: [ifIndex]
        lookup: ifPhysAddress
      - source_indexes: [ifIndex]
        lookup: ifAdminStatus
      - source_indexes: [ifIndex]
        lookup: ifOperStatus
      - source_indexes: [ipAdEntIfIndex]
        lookup: ipAdEntNetMask
      - source_indexes: [ipAdEntIfIndex]
        lookup: ipAdEntBcastAddr
      - source_indexes: [hrStorageIndex]
        lookup: hrStorageType
      - source_indexes: [hrStorageIndex]
        lookup: hrStorageDescr
      - source_indexes: [hrStorageIndex]
        lookup: hrStorageAllocationUnits
      - source_indexes: [hrDeviceIndex]
        lookup: hrDeviceType
      - source_indexes: [hrDeviceIndex]
        lookup: hrDeviceType
      - source_indexes: [hrDeviceIndex]
        lookup: hrDeviceDescr
      - source_indexes: [hrDeviceIndex]
        lookup: hrDeviceStatus
      - source_indexes: [hrSWRunIndex]
        lookup: hrSWRunName
      - source_indexes: [hrSWRunIndex]
        lookup: hrSWRunPath
      - source_indexes: [hrSWRunIndex]
        lookup: hrSWRunParameters
      - source_indexes: [hrSWRunIndex]
        lookup: hrSWRunType
      - source_indexes: [hrSWRunIndex]
        lookup: hrSWRunStatus
      - source_indexes: [laIndex]
        lookup: laNames
      - source_indexes: [cHSMIndex]
        lookup: cHSMValid
      - source_indexes: [cHSMIndex]
        lookup: cHSMDevice
      - source_indexes: [cHSMIndex]
        lookup: cHSMMode
      - source_indexes: [cHSMIndex]
        lookup: cHSMState
      - source_indexes: [cHSMIndex]
        lookup: cHSMTemplate
      - source_indexes: [cHSMIndex]
        lookup: cHSMModuleState
    overrides:
      cslVersion:
        type: DisplayString
      cslPowerSupplyStatusAsString:
        ignore: true
      csSerialNumber:
        ignore: true
      csDevice:
        ignore: true
      csVersion:
        ignore: true
      cslPowerSupplyIndex:
        ignore: true
    max_repetitions: 25
    retries: 3
    timeout: 10s
