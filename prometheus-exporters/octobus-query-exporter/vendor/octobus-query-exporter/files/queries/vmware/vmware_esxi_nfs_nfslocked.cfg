[query_elasticsearch_octobus_nfslocked_issue]
QueryIndices = c0001_log*
QueryOnMissing = drop
QueryJson = {
      "size": 0,
      "aggs": {
          "hostsystem": { "terms": { "field": "syslog_hostname.keyword" }}
      },
      "query": {
          "bool": {
              "must": [
                  { "match_phrase": { "syslog_message": "status: NFS4ERR_LOCKED" }}
              ],
              "filter": [
                  { "term": { "sap.cc.audit.source.keyword": "ESXi" }},
                  { "range": { "@timestamp": { "gte": "now-20m" }}}
              ]
          }
      }
  }
