[query_elasticsearch_rabbitmq_econnrefused]
# The DEFAULT settings can be overridden.
QueryIntervalSecs = 300
QueryTimeoutSecs = 15
QueryOnMissing = zero
QueryIndices = logstash-*
QueryJson = {
              "size": 0,
              "query": {
                "bool": {
                   "must": [
                  {
                    "match" : {
                      "log": "ECONNREFUSED"
                    }
                  }
                  ],
                  "filter": [
                    { "range": { "@timestamp": { "gte": "now-5m" }}}
                  ]
                }
              },
              "aggs": {
                "econnrefused": { "terms": { "field": "kubernetes.namespace_name.keyword", "size": 1000 }},"failed": { "terms": { "field": "kubernetes.container_name.keyword", "size": 1000 }}
              }
            }
