global:
  prometheus:
    targets: infra-collector

netapp-cap-exporter:
  enabled: true
  prometheus:
    scrape: true

netapp-perf-exporter:
  fullname: "netapp-performance-exporter"
  enabled: false
  prometheus:
    scrape: false
