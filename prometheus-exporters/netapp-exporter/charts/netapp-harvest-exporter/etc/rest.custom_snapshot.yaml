name:                     Snapshot
query:                    api/private/cli/snapshot
object:                   snapshot

counters:
  - ^^snapshot               => snapshot
  - ^^volume                 => volume
  - ^^vserver                => svm
  - create_time(timestamp)   => create_time
  - size                     => size

export_options:
  include_all_labels: true
