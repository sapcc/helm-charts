apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "name" . }}
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: -|
    metrics:
{{ concat .Values.metrics .Values.additional_metrics | indent 6 | toYaml }}

