global:
  tld:
  region:
  cluster:
  linkerd_requested: true

owner-info:
  service: aci-exporter
  support-group: observability
  maintainers:
    - Gabriel Rotaru
  helm-chart-url: https://github.com/sapcc/helm-charts/tree/master/prometheus-exporters/aci-suite

aci:
  apic_hosts:
  aci_user:
  aci_password:
  cache_password:

valkey:
  enabled: true

data-collector:
  enabled: true

netbox-collector:
  enabled: true

fault-collector:
  enabled: true

correlator:
  enabled: true
