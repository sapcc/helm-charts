{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.fullname }}
  labels:
    {{- include "netbox-collector.labels" . | nindent 4 }}
type: Opaque
data:
  aci_user: {{ required ".Values.aci.aci_user" .Values.aci.aci_user | b64enc | quote }}
  aci_password: {{ required ".Values.aci.aci_password" .Values.aci.aci_password | b64enc | quote }}
  cache_password: {{ required ".Values.aci.cache_password" .Values.aci.cache_password | b64enc | quote }}
{{- end }}
