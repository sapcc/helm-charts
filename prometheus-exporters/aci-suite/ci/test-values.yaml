global:
  tld: somewhere.wherever.whenever
  region: no-wh-1
  cluster: no-wh-1i
  registry: docker.io

aci:
  apic_hosts: "host1:443,host2:443"

# Pass values to each subchart
data-collector:
  aci:
    apic_hosts: "host1:443,host2:443"
    aci_user: test_aci_user
    aci_password: test_aci_password
    cache_password: test_cache_password

fault-collector:
  aci:
    apic_hosts: "host1:443,host2:443"
    aci_user: test_aci_user
    aci_password: test_aci_password
    cache_password: test_cache_password

correlator:
  aci:
    apic_hosts: "host1:443,host2:443"
    aci_user: test_aci_user
    aci_password: test_aci_password
    cache_password: test_cache_password

valkey:
  aci:
    cache_password: test_cache_password

netbox-collector:
  aci:
    cache_password: test_cache_password