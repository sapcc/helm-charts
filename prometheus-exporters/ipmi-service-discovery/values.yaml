global:
  # imageRegistry:
  ipmi_service_discovery:
    ironic:
      enabled: false
    netbox:
      enabled: false
      # api_token:

  # ipmi_exporter_user:
  # ipmi_exporter_user_passwd:

app_env: "production"
image: monsoon/ipmi-service-discovery
tag: v0.3.14
os_user_domain_name: "Default"
os_project_name: "master"
os_project_domain_name: "ccadmin"
