global:
  mageRegistry: docker.io
  ipmi_service_discovery:
    ironic:
      enabled: true
    netbox:
      enabled: true
      api_token: top Secret

  ipmi_exporter_user: ipmi
  ipmi_exporter_user_passwd: topSecret
