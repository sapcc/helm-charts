global:
  imageRegistry: docker.io
  ipmi_username_cp: ipmi
  ipmi_password_cp: topSecret

  ipmi_exporter:
    ironic:
      enabled: true

  http_sd_configs:
    netbox_url: http://netbox

ipmi_username: ipmi
ipmi_password: topSecret
