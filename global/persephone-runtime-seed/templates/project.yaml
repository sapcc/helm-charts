{{- range $key, $cluster := .Values.persephoneShoots }}
---
apiVersion: core.gardener.cloud/v1beta1
kind: Project
metadata:
  name: pseeds-{{ $cluster.landscapeName }}
spec:
  namespace: garden
{{- end }}
