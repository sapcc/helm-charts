global:
  region: qa-de-1
contextSuffix: qa-de-1
landscapeName: "canary"
keystone:
  application_credential_id: "the id"
  application_credential_secret: "the secret"
workers:
  - name: worker
    machine:
      type: g_c4_m16
    minimum: 3
    maximum: 6
    zones:
      - qa-de-1a
highAvailability:
  enabled: true
floatingPoolName: my-fancy-fips
seedName: mySeedCluster
oidcConfig:
  enabled: true
  jwt:
    - issuer:
        url: https://some-url
        audiences:
          - oidc-client-id
      claimMappings:
        username:
          claim: 'username'
        groups:
          claim: 'groups'
    - issuer:
        url: https://some-url
        audiences:
          - oidc-client-id
      claimMappings:
        username:
          claim: 'username'
        groups:
          claim: 'groups'
