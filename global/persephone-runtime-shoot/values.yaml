landscapeName: "qa" # should be qa, canary or prod

kubernetes:
  # version: x.y.z
  verticalPodAutoscaler:
    enabled: true

highAvailability:
  enabled: false

floatingPoolName: FloatingIP-external-ccadmin

# workers:
# seedName:

oidcConfig:
  enabled: false
  anonymous:
    enabled: true
    conditions:
      - path: /version
  jwt: {}

tolerations:
  whitelist:
  - key: "persephone_test-managedseed"
