global:
  registry: keppel.global.cloud.sap/ccloud
  region: "qa-de-1"
  vaultBaseURL: "https://vault.qa-de-1.cloud.sap"

# secrets:
#   encryptionKey: "dummy-key"

concourse:
  secrets:
    postgresUser: "concourse"    # Fix for strict template
    postgresPassword: "password" # Fix for strict template
    encryptionKey: "dummy-key"

webhookBroadcaster:
  auth:
    username: ""
    password: ""