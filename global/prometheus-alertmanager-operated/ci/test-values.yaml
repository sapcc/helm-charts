global:
  region: regionOne
  domain: evil.corp

slack:
  global:
    webhookURL: topSecret!
    devnullWebhookURL: topSecret!

  channels:
    - name: api
      warningWebhookURL: topSecret!
      criticalWebhookURL: topSecret!
    - name: kubernikus
      criticalWebhookURL: topSecret!
    - name: metal
      warningWebhookURL: topSecret!
      criticalWebhookURL: topSecret!
    - name: vpod
      warningWebhookURL: topSecret!
      criticalWebhookURL: topSecret!
    - name: network
      warningWebhookURL: topSecret!
      criticalWebhookURL: topSecret!
    - name: vmware
      warningWebhookURL: topSecret!
      criticalWebhookURL: topSecret!
    - name: compute
      criticalWebhookURL: topSecret!
    - name: compute_storage_api
      criticalWebhookURL: topSecret!
    - name: email
      criticalWebhookURL: topSecret!
    - name: identity
      criticalWebhookURL: topSecret!
    - name: foundation
      criticalWebhookURL: topSecret!
    - name: network_api
      criticalWebhookURL: topSecret!
    - name: observability
      criticalWebhookURL: topSecret!
    - name: containers
      criticalWebhookURL: topSecret!

octobus:
  # unused
  gymInstance: topSecret!
  # used
  gcpInstance: topSecret!

awx:
  basicAuthUser: topSecret!
  basicAuthPwd: topSecret!
  listenerURL: topSecret!

pagerduty_sap:

  network:
    serviceKey: topSecret!
    serviceKeyCsm: topSecret!

  vmware:
    serviceKey: topSecret!

  osd_mom_network:
    serviceKey: topSecret!

  osd_mom_compute:
    serviceKey: topSecret!

  compute:
    serviceKey: topSecret!

  compute_storage_api:
    serviceKey: topSecret!

  identity:
    serviceKey: topSecret!

  email:
    serviceKey: topSecret!

  containers:
    serviceKey: topSecret!

  network_api:
    serviceKey: topSecret!

  observability:
    serviceKey: topSecret!

alertmanagerName: alertmanagerRegionOne

prometheus-alertmanager:
  name: alertmanagerRegionOne
