apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-global

data:
  {{- $files := .Files }}
  {{ range tuple "kubernetes.rules" "etcd.rules"  "cluster.rules" "prometheus.yaml" "ca.crt" }}
  {{ . }}: |
{{ $files.Get . | indent 4 }}
  {{- end }}
