# Test: only repository is set in values, tag should come from version fallback
# Expected: repository is rendered, tag comes from version (v0.3.0), ref is null
extensions:
  auditing:
    enabled: true
    version: v0.3.0
    helm:
      ociRepository:
        repository: keppel.example.com/auditing-custom
