{{- if .Values.garden.dashboard }}
{{- if .Values.garden.dashboard.enabled }}
{{- if .Values.garden.dashboard.oidc.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: gardener-dashboard-oidc
  namespace: garden
type: Opaque
stringData:
  client_id: {{ .Values.garden.dashboard.oidc.clientIDPublic }}
{{- end }}
{{- end }}
{{- end }}
