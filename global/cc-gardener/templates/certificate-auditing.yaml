{{ if .Values.auditing.enabled -}}
apiVersion: cert.gardener.cloud/v1alpha1
kind: Certificate
metadata:
  name: auditing-mtls-cert
spec:
  secretRef:
    name: auditing-mtls
  issuerRef:
    name: self-signed
{{ end -}}
