kind: Service
apiVersion: v1

metadata:
  namespace: juno
  name: landing-page

spec:
  selector:
    app: landing-page
  ports:
    - name: dashboard
      port: 80
      protocol: TCP
      targetPort: dashboard
